import { useEffect } from 'react'

export default function useScrollAnimation() {
  useEffect(() => {
    if (typeof window === 'undefined') return
    // Placeholder: ajouter IntersectionObserver si besoin
  }, [])
}
